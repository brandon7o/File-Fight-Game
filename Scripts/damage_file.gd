extends Node2D

#Base stats for damage file
var fileEnvironment = "None"
var fileName = "Damage File"
var fileDamage = 5


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	#TODO: Move to turn end signal when created
	#Determines the environment when placed.
	if (true):
		pass
	elif (true):
		pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
